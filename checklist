<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>EEOC Complaint Checklist — EEOC Explained</title>
  <meta name="description" content="Simple, step-by-step EEOC complaint checklist you can save in your browser.">
  <link rel="stylesheet" href="style.css" />
</head>
<body>
  <header class="site-header">
    <div class="brand">
      <img src="Clipped_image_20251006_101225.png" alt="EEOC Explained logo" width="56" height="56" />
      <div>
        <h1 class="site-title">EEOC Explained</h1>
        <p class="site-sub">Plain-English guidance for workers and employers</p>
      </div>
    </div>
    <nav class="navbar" aria-label="Primary">
      <a href="index.html">Home</a>
      <a href="about.html">About</a>
      <a href="checklist.html" class="active">Checklist</a>
      <a href="resources.html">Resources</a>
      <a href="contact.html">Contact</a>
    </nav>
  </header>

  <main class="container">
    <section class="card">
      <h2>EEOC Complaint Checklist</h2>
      <p class="lead">Use this quick checklist to track your progress. Your selections are saved only in your browser.</p>

      <div id="list" class="list"></div>

      <div class="cta-row" style="margin-top:12px">
        <button class="btn" id="saveBtn" type="button">Save Progress</button>
        <button class="btn btn-ghost" id="resetBtn" type="button" aria-describedby="reset-note">Reset</button>
      </div>
      <p id="reset-note" class="muted" style="margin:.6rem 0 0">Reset clears saved progress on this device only.</p>
    </section>

    <section class="grid">
      <article class="card">
        <h3>Tips</h3>
        <ul class="list">
          <li>Write dates right away (deadlines can be short).</li>
          <li>Keep copies of everything you submit or receive.</li>
          <li>Reply to EEOC messages promptly.</li>
        </ul>
      </article>
      <article class="card">
        <h3>Heads-up</h3>
        <p class="muted">This checklist is educational and not legal advice. If you need advice for your situation, consider speaking with an attorney.</p>
      </article>
    </section>
  </main>

  <footer class="site-footer">
    <p>© <span id="y"></span> EEOC Explained — All rights reserved.</p>
  </footer>

  <script>
    document.getElementById('y').textContent = new Date().getFullYear();

    // Checklist data
    const ITEMS = [
      { id: 'file', label: 'File an EEOC Charge', note: 'Submit your charge online, by mail, or in person at your nearest EEOC office.' },
      { id: 'evidence', label: 'Provide Supporting Evidence', note: 'Include emails, notes, witness names, or documents to strengthen your claim.' },
      { id: 'respond', label: 'Respond to Information Requests', note: 'Watch for EEOC communications and reply promptly to stay on track.' },
      { id: 'mediation', label: 'Participate in Mediation (if offered)', note: 'Mediation can resolve your case faster without a full investigation.' },
      { id: 'investigation', label: 'Investigation', note: 'The EEOC may collect documents, conduct interviews, or visit the workplace.' },
      { id: 'final', label: 'Final Determination / Right to Sue', note: 'You may receive results or a Notice of Right to Sue.' }
    ];

    const KEY = 'eeoc-checklist-v1';

    const el = id => document.getElementById(id);
    const list = el('list');

    function render(state = {}) {
      list.innerHTML = ITEMS.map(item => `
        <label style="display:block;padding:10px 0;border-top:1px solid var(--border)">
          <input type="checkbox" id="${item.id}" ${state[item.id] ? 'checked' : ''} />
          <strong style="margin-left:.5rem">${item.label}</strong>
          <div class="muted" style="margin-left:1.85rem">${item.note}</div>
        </label>
      `).join('') + `<div style="border-top:1px solid var(--border)"></div>`;
    }

    function load() {
      try { return JSON.parse(localStorage.getItem(KEY) || '{}'); }
      catch { return {}; }
    }

    function getCurrent() {
      const state = {};
      ITEMS.forEach(i => state[i.id] = el(i.id)?.checked || false);
      return state;
    }

    function save() {
      localStorage.setItem(KEY, JSON.stringify(getCurrent()));
      flash('Progress saved.');
    }

    function reset() {
      localStorage.removeItem(KEY);
      render({});
      flash('Checklist reset.');
    }

    function flash(msg) {
      const n = document.createElement('div');
      n.textContent = msg;
      n.setAttribute('role','status');
      n.style.cssText = 'position:fixed;inset:auto 16px 16px auto;background:#111;color:#fff;padding:10px 12px;border-radius:10px';
      document.body.appendChild(n);
      setTimeout(()=>n.remove(), 1500);
    }

    // Init
    render(load());
    el('saveBtn').onclick = save;
    el('resetBtn').onclick = reset;
  </script>
</body>
</html>